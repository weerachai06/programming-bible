# Mini Redis Example

โปรเจกต์ตัวอย่างการสร้าง Redis server แบบง่ายด้วย Rust และ mini-redis library พร้อมกับ client ที่สามารถเชื่อมต่อและใช้งานได้

## 📋 คุณสมบัติ

- ✅ Redis server ที่รองรับ multiple clients พร้อมกัน
- ✅ รองรับคำสั่ง `SET` และ `GET`
- ✅ เก็บข้อมูลใน memory (HashMap)
- ✅ ใช้ async/await สำหรับ concurrent handling
- ✅ ตัวอย่าง client สำหรับทดสอบการเชื่อมต่อ

## 🚀 การติดตั้งและการใช้งาน

### ความต้องการเบื้องต้น

- [Rust](https://rustup.rs/) (edition 2024)
- Cargo (มาพร้อมกับ Rust)

### 1. Clone และเข้าไปในโปรเจกต์

```bash
cd mini-redis-example
```

### 2. สร้าง project (หากต้องการ)

```bash
cargo new my-redis --bin
cd my-redis
```

### 3. เพิ่ม dependencies ใน Cargo.toml

```toml
[dependencies]
tokio = { version = "1", features = ["full"] }
mini-redis = "0.4"
```

### 4. รัน Redis Server

```bash
cargo run
```

Server จะเริ่มทำงานที่ `127.0.0.1:6379` และจะแสดงข้อความ:
```
Redis server listening on 127.0.0.1:6379
```

### 5. ทดสอบด้วย Example Client

เปิด terminal ใหม่และรันคำสั่ง:

```bash
cargo run --example hello-redis
```

ผลลัพธ์ที่ควรได้:
```
got value from the server; result=Some(b"world")
```

## 🔧 การใช้งาน

### คำสั่งที่รองรับ

| คำสั่ง | รูปแบบ | คำอธิบาย |
|--------|--------|----------|
| `SET` | `SET key value` | เก็บค่าลงใน key ที่ระบุ |
| `GET` | `GET key` | ดึงค่าจาก key ที่ระบุ |

### ตัวอย่างการใช้งานใน Rust Code

```rust
use mini_redis::{Result, client};

#[tokio::main]
async fn main() -> Result<()> {
    // เชื่อมต่อกับ server
    let mut client = client::connect("127.0.0.1:6379").await?;

    // เก็บข้อมูล
    client.set("my_key", "my_value".into()).await?;

    // ดึงข้อมูล
    let result = client.get("my_key").await?;
    println!("Value: {:?}", result);

    Ok(())
}
```

### ทดสอบด้วย redis-cli (หากมีติดตั้ง)

```bash
# เชื่อมต่อ
redis-cli -p 6379

# เก็บข้อมูล
SET username "john_doe"

# ดึงข้อมูล  
GET username
```

## 🏗️ โครงสร้างโปรเจกต์

```
├── Cargo.toml              # การตั้งค่าและ dependencies
├── src/
│   └── main.rs            # Redis server หลัก
├── examples/
│   └── hello-redis.rs     # ตัวอย่าง client
└── README.md              # เอกสารนี้
```

## 📝 อธิบายโค้ด

### Server (`src/main.rs`)

1. **TCP Listener**: รับ connection จาก clients บนพอร์ต 6379
2. **Concurrent Handling**: ใช้ `tokio::task::spawn` สำหรับจัดการ multiple clients
3. **Command Processing**: แปลง frames เป็น commands และดำเนินการตาม command type
4. **In-Memory Storage**: ใช้ `HashMap` เก็บข้อมูล key-value

### Client (`examples/hello-redis.rs`)

1. **Connection**: เชื่อมต่อกับ server ที่ `127.0.0.1:6379`
2. **SET Operation**: เก็บข้อมูล `"hello"` -> `"world"`
3. **GET Operation**: ดึงข้อมูลจาก key `"hello"`

## ⚠️ ข้อจำกัด

- ข้อมูลเก็บใน memory เท่านั้น (จะหายเมื่อปิด server)
- รองรับเฉพาะคำสั่ง `SET` และ `GET`
- ไม่มีการ authentication
- ไม่มีการจัดการ persistence

## 🔄 การพัฒนาเพิ่มเติม

หากต้องการเพิ่มคุณสมบัติ:

1. **เพิ่มคำสั่งใหม่**: เพิ่ม case ใน match statement ใน `process()` function
2. **Persistence**: ใช้ database หรือ file storage แทน HashMap
3. **Authentication**: เพิ่มการตรวจสอบสิทธิ์
4. **Clustering**: รองรับ multiple server instances

## 📚 เอกสารเพิ่มเติม

- [Mini Redis Documentation](https://docs.rs/mini-redis/)
- [Tokio Documentation](https://docs.rs/tokio/)
- [Redis Protocol Specification](https://redis.io/topics/protocol)

## 🤝 การช่วยเหลือ

หากพบปัญหาหรือมีคำถาม สามารถ:

1. ตรวจสอบว่า port 6379 ว่างและไม่ถูกใช้งานโดยโปรแกรมอื่น
2. ตรวจสอบการติดตั้ง Rust และ Cargo
3. รัน `cargo clean` แล้ว `cargo build` ใหม่หากมีปัญหา compilation

---

**สร้างโดย**: Programming Bible Project  
**ภาษา**: Rust  
**License**: MIT